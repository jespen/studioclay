name: 🚀 Deploy website on push

# This workflow will deploy the website to the FTP server on push to main branch
on:
  push:
    branches: [ main ]  # Set a branch to deploy from

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 📝 Create .env.production file
      run: |
        echo "Creating .env.production file..."
        echo "NEXT_PUBLIC_API_URL=https://studioclay.se/api" > .env.production
        echo "NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" >> .env.production
        echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" >> .env.production
        echo "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" >> .env.production
        echo "Environment file created"
        
    - name: 🔍 Debug environment variables
      run: |
        echo "Checking environment variables..."
        echo "NEXT_PUBLIC_SUPABASE_URL exists: $([[ -n ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }} ]] && echo 'yes' || echo 'no')"
        echo "NEXT_PUBLIC_SUPABASE_ANON_KEY exists: $([[ -n ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }} ]] && echo 'yes' || echo 'no')"
        echo "SUPABASE_SERVICE_ROLE_KEY exists: $([[ -n ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }} ]] && echo 'yes' || echo 'no')"
        echo "Contents of .env.production:"
        cat .env.production
      
    - name: 🏗️ Build project
      run: |
        echo "Starting build..."
        echo "Setting environment variables..."
        export NEXT_PUBLIC_SUPABASE_URL="${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}"
        export NEXT_PUBLIC_SUPABASE_ANON_KEY="${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}"
        export SUPABASE_SERVICE_ROLE_KEY="${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}"
        export NEXT_PUBLIC_API_URL="https://studioclay.se/api"
        export NODE_ENV=production
        
        echo "Verifying environment variables..."
        echo "NEXT_PUBLIC_SUPABASE_URL length: ${#NEXT_PUBLIC_SUPABASE_URL}"
        echo "NEXT_PUBLIC_SUPABASE_ANON_KEY length: ${#NEXT_PUBLIC_SUPABASE_ANON_KEY}"
        echo "SUPABASE_SERVICE_ROLE_KEY length: ${#SUPABASE_SERVICE_ROLE_KEY}"
        
        echo "Running build..."
        npm run build
        echo "Build completed"
      
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./out/
        server-dir: ${{ secrets.FTP_SERVER_DIR }}
        dangerous-clean-slate: true  # Use with caution - this will delete all files on the server 